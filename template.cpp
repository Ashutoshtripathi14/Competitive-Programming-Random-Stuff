//my shoulder hurts T_T T_T Hope this passes ^_^
//Tuturu~
//tried something didn't work
//idk man let's see
//:((
#include<stdio.h>
#include<ctime>
#include<stdlib.h>
#include<math.h>
#include<string.h>
#include <iostream>
#include <iomanip>
#include <cmath>
#include <algorithm>
#include <vector>
#include <stack>
#include <map>
#include <unordered_map>
#include <set>
#include <string>
#include <queue>
#include <deque>
#include <tuple>
#include <fstream>

using namespace std;

typedef long long ll;
typedef vector<int> vi;
typedef vector<ll> vl;
typedef pair<ll, ll> pll;

#define rep(i,n) for(ll _n=(n),i=0; i<_n; ++i)
#define FOR(i,a,b) for(ll _b=(b),i=(a); i<=_b; ++i)
#define FORD(i,a,b) for(ll _b=(b),i=(a); i>=_b; --i)

#define fr first
#define sc second
#define pb push_back
#define pii pair<ll, ll>

const ll MOD = 1e9 + 7;
const ll INF = 1e9 + 1;
const ll NINF = -1 * INF;



void solve(){k)<<endl;

}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    ll t;
    cin >> t;

    while(t--){
      solve();
    }
}
